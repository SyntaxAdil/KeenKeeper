"use client"
import useTimeline from '../../../hooks/useTimeline'

const SerachTimeline = () => {

    const {setQuery}=useTimeline()
  return (
    <div>SerachTimeline</div>
  )
}

export default SerachTimeline